<androidx.coordinatorlayout.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/clock_info_bottom_sheet"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    app:behavior_hideable="false"
    app:behavior_peekHeight="@dimen/peek_height"
    app:layout_behavior="app.simple.positional.behaviors.CustomBottomSheetBehavior">

    <ImageView
        android:id="@+id/expand_up_clock_sheet"
        android:layout_width="@dimen/expand_up_image_size"
        android:layout_height="@dimen/expand_up_image_size"
        android:layout_gravity="center_horizontal"
        android:src="@drawable/ic_expand_up"
        app:tint="@color/iconColor" />

    <include layout="@layout/info_panel_clock" />

</androidx.coordinatorlayout.widget.CoordinatorLayout>